# Фикс StatusChip статусов

_Started 2026-05-25 12:16 UTC_

---

## User

КОНТЕКСТ ПРОЕКТА

Система учёта отгрузок овощного сырья от поставщиков на переработку.
Текущее состояние — в файле Wireframes.html в проекте.

ВИЗУАЛЬНЫЙ ЯЗЫК (сохранять во всех новых экранах):

Палитра сырья: огурцы — зелёный, черри — красно-розовый,
томаты — оранжевый, патиссоны — жёлтый, халапеньо — тёмно-зелёный,
перец — красно-зелёный градиент, баклажан — фиолетовый.

Чипы статуса: «Запланировано» — жёлтый + полосатая штриховка строки,
«Отправлено» — зелёный.

Пилюли сырья, оверлейный скроллбар, типографика — как в существующих
экранах.

Tailwind через CDN, иконки lucide или эмодзи.

РОЛИ:

Пользователь: главная (просмотр, поиск, фильтры, колонки,
toggle Таблица/Heatmap, Excel, печать), справочник «Водители»
(просмотр + поиск + фильтр по ТК), модалка карточки водителя,
мобильный просмотр.

Админ: всё вышеперечисленное + создание/редактирование везде,
тара, ингредиенты, контракты, аналитика, остальные справочники.

ПРАВИЛА РАБОТЫ:

Не переписывай существующие экраны, если в задаче явно не сказано
обратное.

Новые экраны добавляй в конец файла, в логических разделах.

Никакой реальной логики — статичные wireframes с моковыми данными.

ЗАДАЧА: критический фикс — после задачи 8.3б («Виджет План vs Факт»)
сломалась главная страница.

Ошибка в консоли браузера:
  analytics.jsx:1345
  Uncaught TypeError: Cannot read properties of undefined (reading 'bg')
      at StatusChip

Стек вызовов: StatusChip → Shipment → VariantA → MainShell.
То есть StatusChip падает при рендере карточек отгрузок на главной.

ПРИЧИНА (вероятная): в задаче 8.3б в компонент StatusChip добавлены
новые статусы для виджета «План vs Факт» («Выполнено», «В процессе»,
«Срыв», «Перевыполнение»), но при этом из словаря стилей пропали или
переименованы старые статусы, используемые на главной
(«Запланировано», «Отправлено»). При рендере старого статуса
styles[status] === undefined, и обращение к .bg падает.

ЧТО НУЖНО СДЕЛАТЬ:

1. Открыть компонент StatusChip (analytics.jsx, строка ~1345
   и далее, ~6193, ~6201).

2. Восстановить в словаре стилей ВСЕ исходные статусы, которые
   использовались до задачи 8.3б:
   - «Запланировано» — жёлтый
   - «Отправлено» — зелёный
   (и любые другие, которые были).

3. Сохранить новые статусы из 8.3б:
   - «Выполнено» — зелёный
   - «В процессе» — жёлтый
   - «Срыв» — красный
   - «Перевыполнение» — оранжевый

4. Добавить защиту: если статус не найден в словаре —
   использовать нейтральный серый стиль (fallback), а не падать
   с undefined.

5. Проверить, что главная страница в обоих режимах
   («Таблица» и «Heatmap») снова рендерится, и виджет
   «План vs Факт» в Аналитике тоже работает.

Никаких других изменений не вносить.

## Assistant

_[tool: list_files]_

_[tool: read_file]_

_[tool: grep]_

_[tool: grep]_

_[tool: read_file]_

_[tool: grep]_

_[tool: grep]_

_[tool: grep]_

_[tool: grep]_

_[tool: grep]_

_[tool: str_replace_edit]_

I can see the issue. In `analytics.jsx`, the `STATUS_META` dictionary only has the new keys (`done`, `progress`, `failed`, `over`) from task 8.3б. The original statuses from the main page (`Запланировано`, `Отправлено`) and any unknown status make `m` undefined, causing `m.bg` to crash.

Let me fix `STATUS_META` to restore the old statuses, keep the new ones, and add a safe fallback in `StatusChip`.Теперь проверю, что главная страница и виджет «План vs Факт» рендерятся без ошибок.

_[tool: done]_

Готово. Исправление в `analytics.jsx`:

1. **Восстановлены исходные статусы** в `STATUS_META`: `'Запланировано'` (жёлтый, ◷) и `'Отправлено'` (зелёный, ✓).
2. **Сохранены новые статусы** из 8.3б: `done`, `progress`, `failed`, `over`.
3. **Добавлен fallback** `STATUS_FALLBACK` (нейтральный серый): `const m = STATUS_META[status] || STATUS_FALLBACK;` — теперь `StatusChip` не падает на неизвестном статусе.

Консоль чистая, главная и аналитика рендерятся.

