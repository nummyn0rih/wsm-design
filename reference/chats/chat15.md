# Уведомления в шапке

_Started 2026-05-25 11:41 UTC_

---

## User

КОНТЕКСТ ПРОЕКТА


Система учёта отгрузок овощного сырья от поставщиков на переработку.
Текущее состояние — в файле Wireframes.html в проекте.


ВИЗУАЛЬНЫЙ ЯЗЫК (сохранять во всех новых экранах):



Палитра сырья: огурцы — зелёный, черри — красно-розовый,
томаты — оранжевый, патиссоны — жёлтый, халапеньо — тёмно-зелёный,
перец — красно-зелёный градиент, баклажан — фиолетовый.

Чипы статуса: «Запланировано» — жёлтый + полосатая штриховка строки,
«Отправлено» — зелёный.

Пилюли сырья, оверлейный скроллбар, типографика — как в существующих
экранах.

Tailwind через CDN, иконки lucide или эмодзи.


РОЛИ:



Пользователь: главная (просмотр, поиск, фильтры, колонки,
toggle Таблица/Heatmap, Excel, печать), справочник «Водители»
(просмотр + поиск + фильтр по ТК), модалка карточки водителя,
мобильный просмотр.

Админ: всё вышеперечисленное + создание/редактирование везде,
тара, ингредиенты, контракты, аналитика, остальные справочники.


ПРАВИЛА РАБОТЫ:



Не переписывай существующие экраны, если в задаче явно не сказано
обратное.

Новые экраны добавляй в конец файла, в логических разделах.

Никакой реальной логики — статичные wireframes с моковыми данными.

ЗАДАЧА: точечная правка — добавить колокольчик уведомлений в
существующую шапку.

В шапке демо-страницы рядом с переключателем темы (или роли)
добавить:

1. Иконку 🔔 с badge — красным кружком и числом непрочитанных
   уведомлений (например, «5»).

2. По клику открывается выпадающий поповер:
   - Заголовок «Уведомления»
   - Две вкладки: «Требуют действия» (3) / «Информационные» (2)
   - Список последних 5 уведомлений: иконка типа, заголовок,
     краткое описание, время («2 ч назад»)
   - Непрочитанные — с точкой слева
   - Внизу кнопка-ссылка «Все уведомления →» (ведёт на страницу
     уведомлений из задачи 8.4б)

3. Типы уведомлений (с иконками):
   - 📦 Тара
   - 🚛 Машина / отгрузка
   - 📊 Качество
   - 📑 Контракт
   - ⚠ Алерт

Колокольчик показать в открытом состоянии (с раскрытым поповером)
для демонстрации.

Никакие другие элементы шапки не трогать.

## Assistant

_[tool: list_files]_

_[tool: read_file]_

_[tool: read_file]_

_[tool: str_replace_edit]_

Now I'll add the notification bell with an opened popover in the demo page header, placed between the role switcher and the user avatar. I'll only touch the `MainShell` top bar — nothing else.Now insert the bell button (with popover) in the top bar, between the role switcher and the avatar:

_[tool: str_replace_edit]_

_[tool: done]_

Готово. В шапке демо-страницы (`MainShell`), между переключателем ролей и аватаром, добавлен 🔔 с красным badge «5» и раскрытым поповером:

- **Заголовок** «Уведомления» + пилюля «5 новых», ссылка «✓ Прочитать всё», крестик закрытия
- **Две вкладки**: «Требуют действия» (3, акцент красный) / «Информационные» (2, акцент синий)
- **5 уведомлений** по типам: ⚠ Алерт (брак), 📦 Тара (низкий остаток), 📊 Качество (нет данных), 🚛 Машина (прибытие), 📑 Контракт (подписан) — каждый с цветной круглой иконкой по типу, заголовком, описанием и временем
- **Непрочитанные** — с красной точкой слева и жёлтой подкладкой строки
- **Низ**: ссылка «Все уведомления →» (заглушка под задачу 8.4б)

Поповер открыт по умолчанию (`useState(true)`) — для демонстрации. Никакие другие элементы шапки не тронуты.

